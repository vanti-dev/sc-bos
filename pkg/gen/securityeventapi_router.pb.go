// Code generated by protoc-gen-router. DO NOT EDIT.

package gen

import (
	context "context"
	fmt "fmt"
	router "github.com/smart-core-os/sc-golang/pkg/router"
	grpc "google.golang.org/grpc"
	io "io"
)

// SecurityEventApiRouter is a SecurityEventApiServer that allows routing named requests to specific SecurityEventApiClient
type SecurityEventApiRouter struct {
	UnimplementedSecurityEventApiServer

	router.Router
}

// compile time check that we implement the interface we need
var _ SecurityEventApiServer = (*SecurityEventApiRouter)(nil)

func NewSecurityEventApiRouter(opts ...router.Option) *SecurityEventApiRouter {
	return &SecurityEventApiRouter{
		Router: router.NewRouter(opts...),
	}
}

// WithSecurityEventApiClientFactory instructs the router to create a new
// client the first time Get is called for that name.
func WithSecurityEventApiClientFactory(f func(name string) (SecurityEventApiClient, error)) router.Option {
	return router.WithFactory(func(name string) (any, error) {
		return f(name)
	})
}

func (r *SecurityEventApiRouter) Register(server grpc.ServiceRegistrar) {
	RegisterSecurityEventApiServer(server, r)
}

// Add extends Router.Add to panic if client is not of type SecurityEventApiClient.
func (r *SecurityEventApiRouter) Add(name string, client any) any {
	if !r.HoldsType(client) {
		panic(fmt.Sprintf("not correct type: client of type %T is not a SecurityEventApiClient", client))
	}
	return r.Router.Add(name, client)
}

func (r *SecurityEventApiRouter) HoldsType(client any) bool {
	_, ok := client.(SecurityEventApiClient)
	return ok
}

func (r *SecurityEventApiRouter) AddSecurityEventApiClient(name string, client SecurityEventApiClient) SecurityEventApiClient {
	res := r.Add(name, client)
	if res == nil {
		return nil
	}
	return res.(SecurityEventApiClient)
}

func (r *SecurityEventApiRouter) RemoveSecurityEventApiClient(name string) SecurityEventApiClient {
	res := r.Remove(name)
	if res == nil {
		return nil
	}
	return res.(SecurityEventApiClient)
}

func (r *SecurityEventApiRouter) GetSecurityEventApiClient(name string) (SecurityEventApiClient, error) {
	res, err := r.Get(name)
	if err != nil {
		return nil, err
	}
	if res == nil {
		return nil, nil
	}
	return res.(SecurityEventApiClient), nil
}

func (r *SecurityEventApiRouter) ListSecurityEvents(ctx context.Context, request *ListSecurityEventsRequest) (*ListSecurityEventsResponse, error) {
	child, err := r.GetSecurityEventApiClient(request.Name)
	if err != nil {
		return nil, err
	}

	return child.ListSecurityEvents(ctx, request)
}

func (r *SecurityEventApiRouter) PullSecurityEvents(request *PullSecurityEventsRequest, server SecurityEventApi_PullSecurityEventsServer) error {
	child, err := r.GetSecurityEventApiClient(request.Name)
	if err != nil {
		return err
	}

	// so we can cancel our forwarding request if we can't send responses to our caller
	reqCtx, reqDone := context.WithCancel(server.Context())
	// issue the request
	stream, err := child.PullSecurityEvents(reqCtx, request)
	if err != nil {
		return err
	}

	// send the stream header
	header, err := stream.Header()
	if err != nil {
		return err
	}
	if err = server.SendHeader(header); err != nil {
		return err
	}

	// send all the messages
	// false means the error is from the child, true means the error is from the caller
	var callerError bool
	for {
		// Impl note: we could improve throughput here by issuing the Recv and Send in different goroutines, but we're doing
		// it synchronously until we have a need to change the behaviour

		var msg *PullSecurityEventsResponse
		msg, err = stream.Recv()
		if err != nil {
			break
		}

		err = server.Send(msg)
		if err != nil {
			callerError = true
			break
		}
	}

	// err is guaranteed to be non-nil as it's the only way to exit the loop
	if callerError {
		// cancel the request
		reqDone()
		return err
	} else {
		if trailer := stream.Trailer(); trailer != nil {
			server.SetTrailer(trailer)
		}
		if err == io.EOF {
			return nil
		}
		return err
	}
}
