// Code generated by protoc-gen-router. DO NOT EDIT.

package gen

import (
	context "context"
	fmt "fmt"
	router "github.com/smart-core-os/sc-golang/pkg/router"
	grpc "google.golang.org/grpc"
	io "io"
)

// TransportApiRouter is a TransportApiServer that allows routing named requests to specific TransportApiClient
type TransportApiRouter struct {
	UnimplementedTransportApiServer

	router.Router
}

// compile time check that we implement the interface we need
var _ TransportApiServer = (*TransportApiRouter)(nil)

func NewTransportApiRouter(opts ...router.Option) *TransportApiRouter {
	return &TransportApiRouter{
		Router: router.NewRouter(opts...),
	}
}

// WithTransportApiClientFactory instructs the router to create a new
// client the first time Get is called for that name.
func WithTransportApiClientFactory(f func(name string) (TransportApiClient, error)) router.Option {
	return router.WithFactory(func(name string) (any, error) {
		return f(name)
	})
}

func (r *TransportApiRouter) Register(server grpc.ServiceRegistrar) {
	RegisterTransportApiServer(server, r)
}

// Add extends Router.Add to panic if client is not of type TransportApiClient.
func (r *TransportApiRouter) Add(name string, client any) any {
	if !r.HoldsType(client) {
		panic(fmt.Sprintf("not correct type: client of type %T is not a TransportApiClient", client))
	}
	return r.Router.Add(name, client)
}

func (r *TransportApiRouter) HoldsType(client any) bool {
	_, ok := client.(TransportApiClient)
	return ok
}

func (r *TransportApiRouter) AddTransportApiClient(name string, client TransportApiClient) TransportApiClient {
	res := r.Add(name, client)
	if res == nil {
		return nil
	}
	return res.(TransportApiClient)
}

func (r *TransportApiRouter) RemoveTransportApiClient(name string) TransportApiClient {
	res := r.Remove(name)
	if res == nil {
		return nil
	}
	return res.(TransportApiClient)
}

func (r *TransportApiRouter) GetTransportApiClient(name string) (TransportApiClient, error) {
	res, err := r.Get(name)
	if err != nil {
		return nil, err
	}
	if res == nil {
		return nil, nil
	}
	return res.(TransportApiClient), nil
}

func (r *TransportApiRouter) GetTransport(ctx context.Context, request *GetTransportRequest) (*Transport, error) {
	child, err := r.GetTransportApiClient(request.Name)
	if err != nil {
		return nil, err
	}

	return child.GetTransport(ctx, request)
}

func (r *TransportApiRouter) PullTransport(request *PullTransportRequest, server TransportApi_PullTransportServer) error {
	child, err := r.GetTransportApiClient(request.Name)
	if err != nil {
		return err
	}

	// so we can cancel our forwarding request if we can't send responses to our caller
	reqCtx, reqDone := context.WithCancel(server.Context())
	// issue the request
	stream, err := child.PullTransport(reqCtx, request)
	if err != nil {
		return err
	}

	// send the stream header
	header, err := stream.Header()
	if err != nil {
		return err
	}
	if err = server.SendHeader(header); err != nil {
		return err
	}

	// send all the messages
	// false means the error is from the child, true means the error is from the caller
	var callerError bool
	for {
		// Impl note: we could improve throughput here by issuing the Recv and Send in different goroutines, but we're doing
		// it synchronously until we have a need to change the behaviour

		var msg *PullTransportResponse
		msg, err = stream.Recv()
		if err != nil {
			break
		}

		err = server.Send(msg)
		if err != nil {
			callerError = true
			break
		}
	}

	// err is guaranteed to be non-nil as it's the only way to exit the loop
	if callerError {
		// cancel the request
		reqDone()
		return err
	} else {
		if trailer := stream.Trailer(); trailer != nil {
			server.SetTrailer(trailer)
		}
		if err == io.EOF {
			return nil
		}
		return err
	}
}
