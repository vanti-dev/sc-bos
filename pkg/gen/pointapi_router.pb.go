// Code generated by protoc-gen-router. DO NOT EDIT.

package gen

import (
	context "context"
	fmt "fmt"
	router "github.com/smart-core-os/sc-golang/pkg/router"
	grpc "google.golang.org/grpc"
	io "io"
)

// PointApiRouter is a PointApiServer that allows routing named requests to specific PointApiClient
type PointApiRouter struct {
	UnimplementedPointApiServer

	router.Router
}

// compile time check that we implement the interface we need
var _ PointApiServer = (*PointApiRouter)(nil)

func NewPointApiRouter(opts ...router.Option) *PointApiRouter {
	return &PointApiRouter{
		Router: router.NewRouter(opts...),
	}
}

// WithPointApiClientFactory instructs the router to create a new
// client the first time Get is called for that name.
func WithPointApiClientFactory(f func(name string) (PointApiClient, error)) router.Option {
	return router.WithFactory(func(name string) (any, error) {
		return f(name)
	})
}

func (r *PointApiRouter) Register(server *grpc.Server) {
	RegisterPointApiServer(server, r)
}

// Add extends Router.Add to panic if client is not of type PointApiClient.
func (r *PointApiRouter) Add(name string, client any) any {
	if !r.HoldsType(client) {
		panic(fmt.Sprintf("not correct type: client of type %T is not a PointApiClient", client))
	}
	return r.Router.Add(name, client)
}

func (r *PointApiRouter) HoldsType(client any) bool {
	_, ok := client.(PointApiClient)
	return ok
}

func (r *PointApiRouter) AddPointApiClient(name string, client PointApiClient) PointApiClient {
	res := r.Add(name, client)
	if res == nil {
		return nil
	}
	return res.(PointApiClient)
}

func (r *PointApiRouter) RemovePointApiClient(name string) PointApiClient {
	res := r.Remove(name)
	if res == nil {
		return nil
	}
	return res.(PointApiClient)
}

func (r *PointApiRouter) GetPointApiClient(name string) (PointApiClient, error) {
	res, err := r.Get(name)
	if err != nil {
		return nil, err
	}
	if res == nil {
		return nil, nil
	}
	return res.(PointApiClient), nil
}

func (r *PointApiRouter) GetPoints(ctx context.Context, request *GetPointsRequest) (*Points, error) {
	child, err := r.GetPointApiClient(request.Name)
	if err != nil {
		return nil, err
	}

	return child.GetPoints(ctx, request)
}

func (r *PointApiRouter) PullPoints(request *PullPointsRequest, server PointApi_PullPointsServer) error {
	child, err := r.GetPointApiClient(request.Name)
	if err != nil {
		return err
	}

	// so we can cancel our forwarding request if we can't send responses to our caller
	reqCtx, reqDone := context.WithCancel(server.Context())
	// issue the request
	stream, err := child.PullPoints(reqCtx, request)
	if err != nil {
		return err
	}

	// send the stream header
	header, err := stream.Header()
	if err != nil {
		return err
	}
	if err = server.SendHeader(header); err != nil {
		return err
	}

	// send all the messages
	// false means the error is from the child, true means the error is from the caller
	var callerError bool
	for {
		// Impl note: we could improve throughput here by issuing the Recv and Send in different goroutines, but we're doing
		// it synchronously until we have a need to change the behaviour

		var msg *PullPointsResponse
		msg, err = stream.Recv()
		if err != nil {
			break
		}

		err = server.Send(msg)
		if err != nil {
			callerError = true
			break
		}
	}

	// err is guaranteed to be non-nil as it's the only way to exit the loop
	if callerError {
		// cancel the request
		reqDone()
		return err
	} else {
		if trailer := stream.Trailer(); trailer != nil {
			server.SetTrailer(trailer)
		}
		if err == io.EOF {
			return nil
		}
		return err
	}
}
