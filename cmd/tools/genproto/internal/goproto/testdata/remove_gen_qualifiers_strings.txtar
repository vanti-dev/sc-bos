-- input.go --
package test

const msg = "gen.Foo and gen.Bar are both types from gen.Package"

func Add(client any) {
	panic(fmt.Sprintf("not correct type: client of type %T is not a gen.FooClient", client))
	_ = "Not an oxygen.Weight or generator.Type"
}
-- want.go --
package test

const msg = "Foo and Bar are both types from Package"

func Add(client any) {
	panic(fmt.Sprintf("not correct type: client of type %T is not a FooClient", client))
	_ = "Not an oxygen.Weight or generator.Type"
}

